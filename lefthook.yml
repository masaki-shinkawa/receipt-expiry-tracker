pre-commit:
  parallel: true
  commands:
    oxlint:
      glob: "*.{js,jsx}"
      run: npx oxlint --fix {staged_files}
      stage_fixed: true
    prettier:
      glob: "*.{js,jsx,json,css,md}"
      run: npx prettier --write {staged_files}
      stage_fixed: true
